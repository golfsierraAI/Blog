---
const { postsMetaData, showMore } = Astro.props;
import BlogCard from "./BlogCard.astro";
---

<div class="p-3 md:p-10 dark:bg-black bg-neutral-100 dark:text-white text-black transition-colors duration-700">
  <h1
    class:list={[
      "text-2xl md:text-3xl font-bold mb-5",
      {
        hidden: !showMore,
      },
    ]}
  >
    Latest posts
  </h1>
  <div class="grid grid-cols-1 md:grid-cols-3 grid-rows-1  dark:text-white text-black transition-colors duration-700 gap-8">
    {
      postsMetaData.map((card) => {
        return (
          <BlogCard
            cardImageUrl={card.imageUrl}
            heading={card.title}
            bodyText={card.description}
            author={card.author}
            tags={card.tags}
            publishedDate={card.date}
          />
        );
      })
    }
    <div
      class:list={[
        "h-full w-full flex justify-center items-center",
        {
          hidden: !showMore,
        },
      ]}
    >
      <a class="font-semibold mt-5" href="/posts">See more</a>
    </div>
  </div>
</div>
